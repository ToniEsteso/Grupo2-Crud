export class ProductoMasCompradoChart {
  name: string;
  points: number;
  color: any;
  bullet: string;
  constructor(name: string, points: number, color: any, bullet: string) {
    this.name = name;
    this.points = points;
    this.color = color;
    this.bullet = bullet;
  }
}
