export class RedSocial {
  id: number;
  nombre: string;
  enlace: string;
  icono: string;
  constructor(id: number, nombre: string, enlace: string, icono: string) {
    this.id = id;
    this.nombre = nombre;
    this.enlace = enlace;
    this.icono = icono;
  }
}
